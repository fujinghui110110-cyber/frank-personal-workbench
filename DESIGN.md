# Design System

## Product Contract

- 每个页面只回答一个主要问题，并且最多突出一个当前页主动作。
- 第一屏必须直接告诉 Frank 现在做什么、为什么，以及下一步如何完成。
- `读取新信息`、`整理新内容`、`需要我处理`是常驻工作流，不得藏进头像、下拉菜单或设置。
- 读取个人微信、企业微信和邮箱只负责取得增量内容；贾维斯整理必须由 Frank 手工启动，两种状态不得混写。
- 默认只展示结论、影响、下一步和 2–4 条必要依据；原文、运行日志、历史细节按需展开。
- 所有操作就地反馈，禁止浏览器确认框、整页刷新、跳回顶部或丢失滚动位置。
- 加载、处理中、离线、失败、空状态和成功必须使用清楚中文，不暴露模型名、英文状态码、JSON、数据库 ID 或内部字段。
- 付款、审批、对外发送、重大规定变更和高风险财务判断始终保留人工确认。

## Workspace Structure

- 左侧常驻今天、事项、规定、搜索、聊天线索、邮件工作。聊天和邮件是 Frank 已确认的高频入口，不得因旧版“四项导航”约束再次隐藏。
- 今天回答“现在先做什么”；事项回答“工作推进到哪里”；规定回答“目前有效口径是什么”；搜索回答“证据在哪里”。
- 聊天线索、邮件工作、业务拍板、跟进人、规定变化和运行状态集中在常驻的“需要我处理”。
- 事项列表使用单列扫描布局；事项详情以业务推进输入和统一时间线为中心。
- 聊天、邮件、规定使用紧凑来源控制头，不使用营销式 Hero 或重复深色大横幅。

## Experience

这是一个在球会办公室强光环境中快速判断财务事项、也会在夜间用手机随手投递材料的工具。界面采用明亮、低眩光、账簿式的产品语汇，让证据和下一步动作先于装饰。

## Visual Direction

- Register: product
- Color strategy: restrained
- Mood: 清晰账簿、海港蓝印章、克制的主动提醒
- Density: 桌面中高密度，手机保留大触控目标
- Radius rule: 控件 10px，面板 14px，标签全圆角
- Motion: 150–220ms，只表达状态变化；减少动态效果时关闭位移

## Color Tokens

```css
--bg: oklch(1 0 0);
--surface: oklch(0.965 0.007 230);
--surface-strong: oklch(0.925 0.012 230);
--ink: oklch(0.205 0.022 230);
--muted: oklch(0.455 0.025 230);
--line: oklch(0.875 0.012 230);
--primary: oklch(0.45 0.086 230);
--primary-strong: oklch(0.37 0.09 230);
--primary-soft: oklch(0.93 0.025 230);
--success: oklch(0.43 0.105 155);
--warning: oklch(0.48 0.115 70);
--danger: oklch(0.49 0.15 28);
--info: oklch(0.49 0.09 245);
```

所有正文与背景达到 WCAG 2.2 AA；状态同时使用文字、图形或位置，不只依赖颜色。

## Typography

- 字体：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif`
- 正文：14–16px / 1.55
- 数据与标签：12–13px / 1.4，保持正常字距
- 页面标题：24–30px，固定字号，不使用流动式超大标题
- 长说明限制在 70ch；事项表格可在桌面展开

## Layout

- Desktop: 240px 左侧导航、弹性主区、可选 320px 证据检查器。
- Mobile: 单列内容、底部四项导航、居中投递按钮；主要触控目标不小于 44px。
- 今日页采用“总控简报 + 有序行动台账”，不使用等权指标卡墙。
- 事项详情以时间线为主，证据、任务、风险、决策和待反馈是同一事项的不同事件类型。

## Core Components

- Intake dock: 粘贴、拖放、文件选择、文字投递；默认先接收再归并。
- Action ledger: 按阻塞、到期、待确认、待反馈排序的行动列表。
- Matter row/detail: 状态、下一步、更新时间、来源数量和完整时间线。
- Evidence reference: 来源、时间、原文定位、事实/推断标记、置信度。
- Review item: 接受、编辑、拒绝，任何推断默认进入待确认。
- Node status: 远端已收件、Mac 待处理、处理中、失败可重试。

## States

- Loading: 与最终布局一致的骨架行。
- Empty: 说明下一步投递动作，不把空数据写成故障。
- Error: 明确材料仍被保留，并提供重试或手动归入。
- Offline: 提示已暂存，联网后自动上传。
- Focus: 2px 可见焦点环；键盘顺序与视觉顺序一致。

## Voice

先写结论和动作，再写依据。使用“已收到”“等待反馈”“需要决策”“依据不足”“查看依据”等明确中文，不使用拟人化营销文案。

## 业务语言

- 页面只说业务结果和下一步，不展示内部处理机制。
- 使用“信息读取情况”“可能有遗漏”“等待整理”“已自动归到原事项”“确认有用”“恢复误过滤”“减少重复事项”“需要复查”。
- 不把同步位置、内部进度标记、检索实现、后台状态值或技术缩写直接交给 Frank 判断。
- 技术字段即使由接口返回，也必须先转换成简洁中文；无法转换时显示“需要检查”，不得原样输出。

## 今日闭环流转台

- 今日页第一屏只有一个最高优先级焦点：“现在做”。
- “接下来三项”严格最多三条，并保持与“现在做”的主次关系。
- 同一主题的个人微信、企业微信、邮件和主动投递在“相关信息已归到一起”中说明，不按来源重复铺卡。
- 闭环状态固定为等待回复、我的承诺、对方承诺、等待审批、需要复联和延期风险。
- 信息读取、贾维斯整理、需要 Frank 处理是三种独立状态；读取完成不代表已经整理。
- 人员视图默认只展示确实存在行动的负责人，不渲染空人员卡片。

## 2026-09-15 审计修复产品约束

- 顶部按钮与处理入口必须有可见文字和键盘焦点；禁止透明覆盖层承载多个不同去向。读取结果按个人微信、企业微信、邮箱分别展示，部分失败不得显示全部完成。
- 待推进、待确认、需检查采用各自的数量与直达目标，不能用全部开放行动数量标作需要拍板。
- 主动投递入口在正常有数据页面也要常驻，不遮挡正文或表单。
- 保留当前跟进台；按事项对接人与按行动负责人是两个独立视角。一条行动允许多人确认负责，长期事项允许只完成一次行动。
- 要求完成日期与下次复盘日期分别保存、分别展示，不得用更改复盘日期延长原要求期限。推进记录与复盘安排同次保存，不因部分失败重复追加。
- 主题区域只显示主题数据；全局统计明确标注范围。不能写死今日到期。来源标签和承诺分类若呈现为控件，必须真的筛选。
- 系统工作原则不冒充学习成果；个人规则须来自实际纠正，能查看来源、停用和恢复，启停应真实改变其适用行为。
- 搜索支持业务关键词及常见业务问题，答案标明依据与缺失信息。没有充分依据时不编造结论，不自动调用付费分析；证据链接须定位原记录。
- 坏图片或语音不能拖住所有正常消息；只有成功提取正文或实际理解的媒体可作为工作判断证据。
