# Frank 的个人工作台：新会话交接文档

最后核对：2026-08-28 01:47（Asia/Shanghai）

## 新会话先复制这句话

> 请先完整读取 `/Users/frank/Documents/Codex/2026-08-09/ai-chatgpt-conversation-6a75943b-5ca4-83ee/PROJECT_HANDOFF.md`，再读取项目根目录的 `AGENTS.md`、`README.md` 和 `DESIGN.md`。先检查当前服务、后台执行节点、API、真实页面和测试状态，再继续维护 Frank 的个人工作台。不要重新搭建，不要自动整理或确认真实业务数据，也不要输出任何密钥、授权码或 Token。

## 1. 项目身份

- 正式名称：`Frank 的个人工作台`
- 项目目录：`/Users/frank/Documents/Codex/2026-08-09/ai-chatgpt-conversation-6a75943b-5ca4-83ee`
- 本机页面：`http://127.0.0.1:8000/#/matters`
- 定位：本地优先、以事项和行动闭环为中心的个人工作操作台。
- 用户界面的 AI 统一称为“贾维斯”。界面不得出现 WorkBuddy、DeepSeek、模型名、JSON、数据库 ID 或工程状态码。
- 项目当前不是 Git 仓库。修改前应先建立可恢复备份，并保护 Frank 的真实业务数据。

## 2. 不可改变的产品边界

- 不使用云主机、域名或公网数据库；工作台只监听本机 `127.0.0.1:8000`。
- Mac 休眠或关机时不处理；安卓材料可在 Syncthing-Fork 投递箱中等待。
- 个人微信、企业微信和邮箱的增量读取可以自动进行。
- AI 整理只能由 Frank 手工点击“整理新内容”启动，避免持续消耗接口额度。
- 不自动确认公司规定、完成事项、审批、付款、记账、对外发送或提交工作日志。
- 高频入口必须常驻可见，不得藏进头像、设置或右上角下拉菜单。
- 高频操作必须就地完成，不使用浏览器确认框，不刷新整页，不跳回顶部，不丢失滚动位置。
- 前端保持原生 HTML、CSS、JavaScript，不引入前端框架。
- 不安装前端 LSP；Frank 已明确拒绝。使用 `node --check`、自动测试和真实浏览器验收。
- 严禁在文档、日志、页面或答复中输出 DeepSeek Key、邮箱授权码、Worker Token、MCP Token。

## 3. 已经落地的系统能力

### 信息来源

- 个人微信：通过 CipherTalk 读取监听中的会话，增量同步、去重，只处理监听范围。
- 企业微信：可以读取群聊和私聊；新增活跃会话会进入监听范围，需继续留意版本变化导致的兼容问题。
- 邮箱：QQ 邮箱 IMAP 只读接入；工作邮箱邮件通过转发进入 QQ 邮箱。非工作邮件过滤后不备份正文和附件。
- 主动投递：网页与安卓 Syncthing-Fork 投递箱支持文件、图片、录音、视频等材料。
- Mac Worker：由 `com.finance-workbench.worker` LaunchAgent 常驻，负责读取、同步和本地处理队列。

### AI 整理

- 文本模型配置为 `deepseek-v4-flash`，图片理解配置为 `deepseek-v4-flash-vision-exp`。
- 用户界面只显示“贾维斯”，不得暴露模型供应商或模型名称。
- 默认分析并发为 300，上限 500；实际并发按等待数量收缩。
- 一批材料先完成分析，再统一执行一次归并，避免逐条生成大量重复事项。
- 整理失败的材料必须出现在“需要检查的内容”中，并支持单条重试。
- 图片、语音或附件只有在 OCR、视觉理解、转写或正文解析成功后，才能生成工作线索或规定候选；不能依据占位符判断。

### 事项与行动

- 事项支持闭环日期、状态修改、业务推进记录和统一时间线。
- 时间线容纳原始要求、聊天、邮件、会议、附件、推进记录、责任人变化、提醒、拍板和完成证明。
- 行动区分需要我做、等待别人、被阻塞、等待拍板、已完成和无需继续。
- 已完成事项不得出现在合并目标中；同一开放事项的后续证据应优先追加或合并。
- 今日页自动排序，但不会自动执行真实业务动作。
- 负责人建议必须由 Frank 确认后才能成为正式负责人。

### 人员体系

- 我自己：Frank / 企微“傅京晖”。
- 孙庆：Hank、hank、孙总；片区财务领导。
- 李静：李姐、静姐；仓管员。
- 欧波：欧哥；仓管员。
- 冯李香：李香、香姐；采购。
- 陈贞婷：阿婷；出纳、兼职文员。
- 潘朝荟：应收、收入、资产管理、收入审计。
- 朱青霞：朱青霞-球会总账；总账主管。
- 单纯点名、寒暄、发送人身份或岗位相符不能生成负责人建议；必须存在明确行动表达。

### 公司规定与 Obsidian

- 规定类型包括新增、修订、废止、解释口径、补充证据、临时任务和非规定。
- 没有工作台旧规定时也能直接收录新规定；修订和解释口径不强制关联旧规定。
- 有明确匹配时更新原规定；关联已有规定是次级操作，不得强迫用户选择。
- 正式目录：`/Users/frank/知识库/04-制度资料/公司最新规定/`
- 已验证写入的当前有效规定：
  - `/Users/frank/知识库/04-制度资料/公司最新规定/10-当前有效/印章管理/印章使用流程中借用领导证件的明确要求.md`
  - 历史版本：`/Users/frank/知识库/04-制度资料/公司最新规定/90-历史版本/印章管理/印章使用流程中借用领导证件的明确要求/v1-20260826.md`
  - 总览：`/Users/frank/知识库/04-制度资料/公司最新规定/00-公司最新规定总览.md`

### 工作日志链路

- 已有聊天增量输出和 Obsidian 工作日志链路。
- 相关 skill：`obsidian-wechat-increment`、`golf-finance-worklog-generator`、`zhonghaitong-worklog`。
- 工作台本身不得未经 Frank 明确确认就向中海通提交工作日志。

## 4. 当前前端产品合同

- 一级导航固定为：事项推进、规定、搜索；工作概览保留为内部能力，不再作为主导航或事项返回层级。
- 顶部常驻三步工作流：读取新信息、整理新内容、需要我处理。
- “读取新信息”与“整理新内容”必须是两种清楚独立的状态。
- 今日第一屏回答：现在做什么、接下来三项、正在等待、延期风险、需要我处理。
- 事项采用单列扫描列表；详情以时间线为中心。
- 聊天、邮件和规定页使用紧凑来源控制头，不使用营销式大型横幅。
- 所有页面使用中文、冷静、信任优先的表达；桌面端为主要验收表面。
- 深色侧栏、浅色画布、单一绿色业务强调色；危险色只用于真实风险。
- 当前静态资源缓存版本：`frank-personal-workbench-shell-v72`。
- “事项推进”不再经过单独的卡片清单中转页；点击主导航后直接进入“左侧全部事项、右侧当前办理”的统一工作区。
- 打开事项后保留左侧连续办理清单，不显示返回清单或返回工作概览的按钮；事项 URL 保留 `#/matters/{id}`。
- 详细规范以项目根目录 `DESIGN.md` 为准。

## 5. 关键代码位置

- 后端入口：`app/main.py`
- 主要业务服务：`app/services.py`
- 公司规定：`app/policies.py`
- 负责人关联：`app/assignees.py`
- Mac Worker：`scripts/mac_worker.py`
- AI 接口：`scripts/deepseek_client.py`
- AI 整理流程：`scripts/workbuddy_analysis.py`（历史文件名，界面仍只能称“贾维斯”）
- 个人微信同步：`scripts/wechat_sync.py`
- 企业微信同步：`scripts/wecom_sync.py`
- 邮箱同步：`scripts/email_sync.py`
- 前端：`app/static/index.html`、`app/static/app.js`、`app/static/app.css`、`app/static/sw.js`
- 测试：`tests/`
- 本地数据和对象：`data/`（运行环境也可能使用 Docker 持久卷，先核对实际配置，禁止凭路径猜测后覆盖）

## 6. 2026-08-26 本次交接时的已验证状态

以下状态会变化，新会话必须重新检查：

- Docker `workbench` 容器正在运行，并绑定 `127.0.0.1:8000`。
- macOS LaunchAgent `com.finance-workbench.worker` 状态为 running。
- 今日简报 API 可以返回真实数据。
- 完整测试通过：190 项测试全部通过，仅有一个第三方 Starlette/httpx 弃用警告。
- 事项推进是默认工作面：左侧常驻“全部推进事项”，按“当前需要推进”和“持续跟进”直接分组，正式数据只读验收为 47 项；右侧原地办理，切换事项只更新右侧内容并记录当前事项地址，不经过额外页面或返回层级。
- 主导航以“事项推进”为唯一日常办理入口，不再展示“工作概览”；事项切换始终在左侧清单和右侧办理区之间原地完成。
- `node --check app/static/app.js` 通过。
- `node --check app/static/sw.js` 通过。
- 项目已纳入 Git，当前维护分支为 `feature/human-workflow-v2`，远程 PR 为 GitHub #1。
- 上一份业务快照曾显示：36 个事项、18 条开放行动、8 位人员、AI 等待整理 0、当前有效规定 1、规定待确认 0。该快照仅用于背景，不得在新会话中当成当前数据。

## 7. 新会话启动检查

进入项目目录后依次执行：

```bash
cd /Users/frank/Documents/Codex/2026-08-09/ai-chatgpt-conversation-6a75943b-5ca4-83ee
docker compose ps
launchctl print gui/$(id -u)/com.finance-workbench.worker
.venv/bin/pytest -q
node --check app/static/app.js
node --check app/static/sw.js
```

然后检查真实页面与 API：

- `http://127.0.0.1:8000/#/matters`
- `http://127.0.0.1:8000/#/policies`
- `http://127.0.0.1:8000/#/search`
- `http://127.0.0.1:8000/#/wechat`
- `http://127.0.0.1:8000/#/email`
- `http://127.0.0.1:8000/#/reviews`
- `http://127.0.0.1:8000/#/nodes`

不要仅凭测试或读代码宣称完成。涉及前端交互时必须在真实浏览器中点击验证；涉及 Obsidian 时必须核对实际文件和内容。

## 8. 继续开发时的工作原则

1. 先读取 `AGENTS.md`、本文件、`README.md` 和 `DESIGN.md`。
2. 先检查真实状态，不把旧会话快照当成当前事实。
3. 保留现有架构和真实数据，不重新搭建、不导入示例数据。
4. 修复根因，复用已有接口和组件，避免继续叠加补丁 CSS 或平行数据结构。
5. 修改前建立可恢复备份；不要覆盖用户数据。
6. 先跑与改动相关的测试，再跑完整测试、前端语法检查和真实操作验收。
7. 不主动处理、确认或提交真实业务材料，除非 Frank 对该具体动作明确授权。

## 9. 当前最值得继续观察的风险

- 企业微信升级可能改变本地数据格式或私聊读取行为；出现异常时先停止推进游标并重新验证。
- 前端 Service Worker 可能导致旧缓存；修改静态文件后同步升级缓存版本并在真实浏览器确认。
- 高并发分析受接口限流和本机任务量影响；失败内容必须可见、可重试且不能丢失。
- 事项自动合并要优先减少重复，但不能跨责任人、义务、截止时间或业务对象误合并。
- README 已改为当前“事项推进”入口、贾维斯建议权限和本机口令不入库的说明；后续改动仍应同步更新文档。

## 10. v2 最终验收状态（2026-08-28 01:05）

- 事项推进采用左侧“全部推进事项”清单和右侧连续办理区；正式只读核对显示 47 项进行中事项。
- 旧 `#/today` 会在同一次渲染中直接进入 `#/matters`，不再出现地址已改变但仍显示“今天”页的情况。
- 静态资源、Service Worker 注册和缓存清单已统一升级为 v72。
- AI 只写待确认依据和办理方案草稿，不自动创建、完成或修改正式行动、提醒、事实与事项状态。
- 工作包只有人工选择并采纳后才创建行动；事项关闭前必须处理或说明全部开放子项。
- 人工修改和撤销使用版本校验；材料同事项重归属不写假审计，真实移动更新时间单调递增。
- 完整测试 209 项通过，前端 JavaScript 与 Service Worker 语法检查通过。
- MCP 只保留读取、分析任务和工作包准备权限；不能投递或归属材料，也不能接受微信候选或处理正式业务状态。
- Chrome 桌面端真实页面已验证旧地址规范化、完整事项清单、事项切换和滚动位置保留；按 Frank 要求未做手机端验收。
- 逐项证据见 `docs/Frank工作台v2验收证据.md`。
- 正式数据已备份到 `backups/20260828-0105-v2-final/`；正式业务数据验收全程只读。

## 11. 旧检查点

上一轮完整检查点仍保留在：

`/Users/frank/.gstack/projects/ai-chatgpt-conversation-6a75943b-5ca4-83ee/checkpoints/20260826-121723-frank-personal-workbench-handoff.md`

它可作为补充历史，但新会话应优先读取项目根目录中的本文件。
